# Tutorials

End-to-end walkthroughs of common workflows.

1. **[Migrate SQL Server → Databricks](migrate-sql-server.md)** — assessment + DDL transpilation + Medallion design via `/migrate`.
2. **[Build a Medallion Pipeline](medallion-pipeline.md)** — Bronze → Silver → Gold on Databricks Unity Catalog using `/spark` and `/pipeline`.

Each tutorial is reproducible from a clean clone given `.env` is set up. They reference real KB entries and skills.
