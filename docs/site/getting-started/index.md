# Getting Started

Three pages, ~5 minutes of reading:

1. **[Installation](installation.md)** — `pip install ai-data-agents`, what each extra gives you, conda environment.
2. **[First Query](first-query.md)** — `.env` setup, `ai-data-agents "your prompt"`, expected output, common errors.
3. **[Slash Commands](slash-commands.md)** — `/sql`, `/spark`, `/fabric`, `/migrate`, `/quality`, `/governance`, etc.

If you're upgrading from v2.x, jump directly to **[Migration v2 → v3](../migration/v2-to-v3.md)** — the imports broke.
